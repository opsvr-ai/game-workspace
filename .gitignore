# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Prisma
apps/server/prisma/*.db
apps/server/prisma/*.db-journal

# Coverage
coverage/

# Tool artifacts
.codegraph/
.understand-anything/
.superpowers/
.remember/

# Docker data
docker/data/

# Uploads
uploads/

# Misc
.cache/
tmp/
temp/
*.exe
apps/agent/agent

# AI assistant artifacts
context-snapshot.json
firebase-debug.log

# Claude Code
.claude/cache/
.claude/metrics/
.claude/logs/

# AXME Code
.axme-code/sessions/
.axme-code/audit-logs/
.axme-code/active-sessions/
.axme-code/worklog.jsonl

# Aura
.aura/

# Build artifacts (too large for git)
apps/server/web-dist/
apps/web/public/app.zip

# Nested AI artifacts
apps/server/.claude/
apps/companion-electron/renderer-dist/
apps/watchdog-service/*.exe
# Electron packaging artifacts
apps/app.zip
apps/app (1).zip
apps/release/
apps/win-unpacked/
apps/chrome_100_percent.pak
apps/chrome_200_percent.pak
apps/d3dcompiler_47.dll
apps/ffmpeg.dll
apps/icudtl.dat
apps/libEGL.dll
apps/libGLESv2.dll
apps/LICENSE.electron.txt
apps/LICENSES.chromium.html
apps/locales/
apps/resources/
apps/resources.pak
apps/snapshot_blob.bin
apps/v8_context_snapshot.bin
apps/vk_swiftshader.dll
apps/vk_swiftshader_icd.json
apps/vulkan-1.dll
apps/companion-config.json
apps/companion-electron/app.asar
apps/companion-electron/src/app.zip
apps/companion-electron/src/win-unpacked/
apps/companion-electron/src/api/client.ts.local-bak

# Local deploy / test scratch
.deploy-tools/
.gocache/
.update-fail-test/
.claude/settings.local.json

# AI assistant working memory / audit (machine-local)
.claude/
.axme-code/
.aura/
memory/
mcp-audit.jsonl
SCRATCHPAD.md
apps/server/.aura/
apps/web/.claude/

# Repo-root scratch: one-off debug dumps / temp scripts / audit screenshots
# （历史遗留：_last.txt、_ws_events.txt、.tmp-*.py 这类一次性文件曾经直接堆在仓库根目录）
/_*.txt
/_*.out
/.tmp-*
/.tmp_*
/.audit/

# electron-builder 的资源目录（directories.buildResources）。
# 注意：上面的 `build/` 会连这里一起忽略，而 `installer.nsh` 恰恰是**真正被 electron-builder
# 加载**的装机脚本 —— 2026-09-22 就是因为它被忽略、改错了同名文件在仓库根，
# 导致「装完找不到目录 / 回传版本号 unknown」查了很久。这里显式放行，避免再被忽略。
!apps/companion-electron/build/
!apps/companion-electron/build/installer.nsh

# scripts/ 下只保留发版/运维链路入库（见 scripts/README.md）。
# 以前这里堆了 440 多个一次性查询/改数据/临时补丁脚本，git status 永远是几百个未入库文件。
# 一次性脚本一律走这个忽略规则，别再入库；新增长期脚本记得加白名单。
scripts/_*
!scripts/_deploy_server_cloud.py
!scripts/_deploy_web_cloud.py
!scripts/_publish_client.py
!scripts/_set_web_version.py
!scripts/_upload_sh_cloud.py
!scripts/_push_watchdog_all.py
!scripts/_repack_client_zip.py

# 仓库根目录遗留的「一次性修复脚本」（2026-09-22 清理：这些都是一次性的排障脚本，
# 事情做完就没用了；正式装机入口是 陪玩端一键安装.bat / .ps1 + scripts/ 下的发版链路）
/deploy-new.bat
/deploy-unpacked.bat
/fix-cloud-config.bat
/fix-git-permissions.ps1
/fix-remote.ps1
/remote-fix.bat
/install-chunlv.bat

# 任何目录下的临时文件
**/.tmp-*
