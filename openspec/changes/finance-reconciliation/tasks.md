## 1. 数据模型与迁移

- [ ] 1.1 在 `schema.prisma` 将金额字段 Float→Int(分)，新增 `PriceRule`、`MerchantPaymentRecord`、`CommissionRule`、`CommissionLedger`、`SettlementSnapshot`、客户画像/风险评分/证据拼图字段
- [ ] 1.2 编写迁移 SQL：存量金额 `round(v*100)` 转换，新增表与索引
- [ ] 1.3 在 `packages/shared` 定义金额类型、价格常量、分成阶梯常量、营业日/异常阈值/截图间隔常量
- [ ] 1.4 更新 `CHANGELOG.md`、`docs/ARCHITECTURE.md`、`docs/USER_MANUAL.md`

## 2. 金额与营业日基准

- [ ] 2.1 实现 `common/money.ts` 元↔分转换纯函数与金额校验
- [ ] 2.2 实现 `common/business-day.ts`：`businessDayOf(date)`、`settlementMonthRange(month)`
- [ ] 2.3 改造 `orders`、`billing`、`companions`、`stats` 模块的金额读写走整数分
- [ ] 2.4 将全部统计/结算查询从自然日切换到营业日范围，补单元测试

## 3. 价格规则、事前声明与应收基准

- [ ] 3.1 实现 `PriceRule` 的 CRUD 与默认价格回退
- [ ] 3.2 实现事前声明约定价（首单≥底价、续单≥续单下限，缺声明回退底价）
- [ ] 3.3 在订单 DONE 流程计算应收基准 `声明价 × 时长` 并落库
- [ ] 3.4 实现转账截屏合计 vs 上报 vs 应收基准校验（合计不符或低于基准标红）
- [ ] 3.5 实现低于应收基准标红、加价部分标记逻辑
- [ ] 3.6 管理端增加价格规则配置页与订单应收核对 UI

## 4. 客户画像、AI 异常分析与私单风险

- [ ] 4.1 实现客户画像计算（模式/单价区间/周月消费/复购频率/价值分级）
- [ ] 4.2 实现单价低于历史基线、模式突变、时长腰斩检测
- [ ] 4.3 实现消费频率与复购率突降检测
- [ ] 4.4 实现 AI 异常分析文案生成（模式/单价/时长/消费变化 + 疑似原因）
- [ ] 4.5 实现按风险评分排序的管理端「重点查看」推送队列
- [ ] 4.6 实现陪玩私单风险评分聚合与抽查池
- [ ] 4.7 实现向陪玩展示客户画像与「重点追踪」激励/降权逻辑
- [ ] 4.8 管理端增加客户分析与私单风险工作台

## 5. 服务证据拼图

- [ ] 5.1 实现开始服务声明卡图片生成（模式/单价/时长/客户/陪玩/时间）作为首图
- [ ] 5.2 实现定时截图（默认 20 分钟/每小时不低于 3 张）并叠加时间戳，本地缓存
- [ ] 5.3 实现结束服务时批量上传本地截图，失败自动重试
- [ ] 5.4 实现 AI 异常分析卡图片生成作为末图（正常显示「未见异常」）
- [ ] 5.5 实现首图 + 中间截图 + 末图合并长图存储，对所有管理者角色开放查看

## 6. 员工收款码对账

- [ ] 6.1 为 `StudioPaymentAccount` 增加 `companionId` 绑定与绑定接口
- [ ] 6.2 实现 `MerchantPaymentRecord` 导入/登记接口与页面
- [ ] 6.3 实现「陪玩 × 营业日」全通道净额核销与差额标红
- [ ] 6.4 对账结果纳入订单与陪玩风险标记汇总

## 7. 客服/店长提成

- [ ] 7.1 实现 `CommissionRule` 配置接口与页面
- [ ] 7.2 复用 `stats` 口径实现提成计算服务
- [ ] 7.3 实现月度提成结算与 `CommissionLedger` 快照（幂等）
- [ ] 7.4 管理端增加提成结算预览与确认流程

## 8. 分成结算落地

- [ ] 8.1 统一阶梯口径为 5200/10000 + 6 个月工龄门槛
- [ ] 8.2 实现 `SettlementSnapshot` 结算服务（TIERED/FIXED，幂等）
- [ ] 8.3 替换 `runMonthlySettlement` 即席计算，生成不可变快照
- [ ] 8.4 退款凭证强制校验接入结算核销

## 9. 报账审核协商

- [ ] 9.1 实现报账提交后的审核金额计算与「通过/待协商」自动分类
- [ ] 9.2 实现管理端「通过并备注 / 退回补充 / 提出改低」操作与留痕
- [ ] 9.3 实现改低金额的原因/凭证校验与陪玩确认流程
- [ ] 9.4 实现协商升级到店长/老板复核
- [ ] 9.5 实现最终金额锁定、审计记录与改低次数风险累计

## 10. 防私单证据链与验证

- [ ] 10.1 汇总截屏时间戳、黑屏检测、应收差额、转账差额、到账差额、私单风险分为风险标记
- [ ] 10.2 管理端增加对账/风险复核工作台
- [ ] 10.3 编写关键计算（营业日、应收基准、阶梯、提成、对账、画像异常、AI 文案）单元测试
- [ ] 10.4 测试库执行迁移与结算演练，抽样比对金额，保留测试数据


