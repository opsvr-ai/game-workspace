{
  "name": "chunlv-esports",
  "private": true,
  "version": "0.0.0",
  "description": "Chunlv Esports Companion Dispatch Management System",
  "scripts": {
    "dev:web": "pnpm --filter @chunlv/web dev",
    "dev:server": "pnpm --filter @chunlv/server dev",
    "build": "pnpm --filter @chunlv/shared build && pnpm --filter @chunlv/server build && pnpm --filter @chunlv/web build",
    "db:migrate": "pnpm --filter @chunlv/server db:migrate",
    "db:seed": "pnpm --filter @chunlv/server db:seed",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "deploy": "git pull && pnpm install && pnpm --filter @chunlv/shared build && pnpm --filter @chunlv/server build && fuser -k 3001/tcp; pm2 restart chunlv-server --update-env",
    "deploy:full": "git pull && pnpm install && pnpm build && fuser -k 3001/tcp; pm2 restart chunlv-server --update-env"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.64.0",
    "@typescript-eslint/parser": "^8.64.0",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.5"
  }
}
