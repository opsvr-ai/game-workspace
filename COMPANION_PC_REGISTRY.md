# 陪玩电脑账号登记表（远程连接/排查用）

规则：查人、连电脑一律用「账号 loginAccount」或「MAC」定位，不要用 IP。IP 会变（DHCP 重分配），MAC 才是硬件唯一标识。
重要教训：局域网 IP 是动态的，每次远程前先在中继机用 `ip neigh`/ARP 按 MAC 反查当前 IP，不要用登记表里的旧 IP。

【硬性约束 2026-09-06】用户明确：以后只开「云服务器」，不开「中继器」。客户端一律直连云服务器（http://1.117.229.36:3001），
禁止再依赖中继机(192.168.0.106)去 atexec 远程重启/查进程。因此客户端必须能自愈（已加轮询优先 + 掉线自愈看门狗），不能靠人手动远程拉。
数据来源：云数据库 ManagedPC（自动按 MAC -> 登录账号 -> IP 登记）。

已登记陪玩电脑：

- 王辰浩：companionId 2621a577-b246-43e6-a004-49369cdc8df7，MAC 4e:de:75:6f:32:6a，最近 IP 192.168.0.193（局域网）
- 王昊：companionId cad6ec80-bf03-4e82-8eb8-9be46c0f8fd6，MAC 34:5a:60:71:68:9f，IP 192.168.0.176（局域网，IP 已从 .134 变过）
- 王甲振：companionId f95ef1a6-af1a-4dd3-8ee4-4f35e803bc6d，MAC a0:ad:9f:14:83:78，IP 192.168.0.196（局域网）
- 秦硕：companionId 55718c00-c6a6-4154-93d4-83f79e0d44dc，MAC 00:e0:4c:92:0b:38，IP 192.168.0.111（局域网）
- 庄元：companionId d759705c-f77a-4e6d-82ba-a6217411f59e，MAC 9c:6b:00:72:df:b6，IP 192.168.0.153（局域网）
- 徐泽宁：companionId 0159a7fa-8547-4b9c-95ea-1a5320b7f6da，MAC 00:e0:97:26:4e:84，IP 192.168.0.10（已离职）
- 周达：companionId e75549fb-f83d-4fd4-9489-c1d00deeae79，MAC 50:eb:f6:ee:0d:7f，IP 122.6.119.6（外网）
- 宋树祥：companionId 3cc12e92-65f3-4941-b9b5-b07d6d681c75，MAC d8:5e:d3:9a:56:6f，IP 122.6.119.7（外网）
- 张权：companionId dd7d8945-90be-4b4f-b3b3-fc24b25c8d7b，MAC 34:5a:60:f4:46:b6，IP 122.6.114.239（外网）
- 张钊：companionId a31cfc27-6aa3-4cb4-8675-72754633199c，MAC 22:23:5c:03:2c:3c，IP 122.6.112.252（外网）
- 葛明东：companionId c8116485-0981-4a14-9460-913dd4f33127，MAC 未登记，IP 183.11.21.93（外网）
- 徐建业：管理机，MAC 10:ff:e0:c2:60:ea，IP 192.168.0.150
- 秦伟杰：companionId 64a6393a-8030-4efa-8b29-01e8ab1fc308，MAC a0:ad:9f:14:7b:f8，主机名 USER-20240831VS，
  局域网 IP 192.168.0.127；运维账号 chunlvops / Chunlv!xhqmvt2（2026-09-24 修复脚本当场生成并自动回传，已实测能登录）

尚未登记 MAC 的陪玩（首次登录会自动登记）：
刘志成、叶号、李德胜、许杰、测试注册许杰、钱鸿鸣、韩爱学、韩磊

远程连接方式：
- 局域网电脑：通过中继机/ARP 表按「账号」或「MAC」定位，不按 IP。
- 外网电脑：IP 是公网出口可能随时变，优先按「账号」在 ManagedPC 查最新 IP。

---

## 机房机队实测快照（2026-09-24 01:00~01:20，扫描 192.168.0.0/24）

登记表里的 IP 是历史值、会漂；**这一节是当天实测的当前对应关系**，下次排查可以拿它当起点，
但认人仍以「机器上客户端 config.json 里的 currentUsername」（`C:\Users\<账号>\AppData\Roaming\@chunlv\companion-electron\config.json`）
或 MAC 为准。当天的探测方式：局域网 ping 扫描（12 台上线）→ 逐台读客户端配置 + 安装目录 + 桌面快捷方式。

| 局域网 IP | 陪玩 | 安装目录 | 当天状态 |
|---|---|---|---|
| 192.168.0.100 | 马凝初 | `C:\Program Files\蠢驴电竞` | 客户端 exe 整个不见了（点不开）→ 当天已修，10 分钟后心跳恢复 |
| 192.168.0.124 | 钱鸿鸣 | `C:\Program Files\@chunlvcompanion-electron` | 正常（1.0.20260930）；当天补了最新看门狗 |
| 192.168.0.126 | 张钊 | `C:\Program Files\@chunlvcompanion-electron` | 正常 |
| 192.168.0.142 | 徐建业 | `C:\Program Files\陪玩管理` | 客户端还停在 9/13 那版 → 当天已修（客户端处于未登录状态） |
| 192.168.0.153 | 童祥瑞 | `C:\Program Files\陪玩管理` | 正常 |
| 192.168.0.154 | 周达 | `C:\Program Files\@chunlvcompanion-electron` | 正常；当天补了最新看门狗 |
| 192.168.0.178 | 王昊 | `C:\Program Files\蠢驴电竞` | 正常 |
| 192.168.0.179 | 陈佳祺 | `C:\Program Files\@chunlvcompanion-electron` | 老板报的那台 → 当天已修，心跳恢复 |
| 192.168.0.196 | 王甲振 | `C:\Program Files\陪玩管理` | 正常（`Program Files\蠢驴电竞` 是旧目录残留） |
| 192.168.0.197 | 张权 | `C:\Program Files\蠢驴电竞` | 正常（共享里只有 IPC$，没有 C$，读文件得走 WMI/--nooutput） |
| 192.168.0.152 | — | — | 只开 80，不是陪玩电脑（路由器/网络设备之类） |

- 当天这套机房的公网出口是 `122.6.117.75`（云端看到的来源 IP），所以局域网机的 IP 不能靠公网 IP 反推。
- `192.168.0.100` 与 `.197` 两台用的是较早一批本地账号密码（`chunlvops` / `Chunlv!j6sA5f8`），
  其余 8 台是 `chunlvops` / `Chunlv@Ops2026`；SMB 登录要按机器分别试。
- 当天 24 小时内全站没有新订单（最新一单 09-22 16:20），因此这轮修复不涉及「接单中不打断」。
- 秦伟杰的机器**不在这 10 台里**：他最后一次上报是 09-22 01:16（版本 1.0.20260924），之后一条都没有 ——
  机器要么没开、要么客户端已经打不开。要修必须先拿到他那台的 IP 或让他在本机跑一键修复。
- 修复会在安装目录旁边留一份「`.broken-<时间>`」旧目录当证据/回滚用（每台约几百 MB，确认新装没问题后可自行删掉）。

---

## 2026-09-24 秦伟杰那台（老板给的地址 `192.168.0.127`）

老板原话：「秦伟杰也打不开，而且图标都变了」→「秦伟杰的 ip 是 192.168.0.127」。

**这台机器从来没有跑过我们的装机脚本**，所以以前既连不进去、也看不到现场：

- `CompanionPC`：秦伟杰 companionId `64a6393a-8030-4efa-8b29-01e8ab1fc308`，客户端 `1.0.20260924`，
  最后一次心跳 **2026-09-22 01:16:51（北京时间 09-22 09:16）**，之后一条都没有 —— 版本正好停在
  09-21 22:37 发的那一版，和「自动更新下发烂字节把客户端装残」那次事故（09-23 已修根因）时间线吻合。
- `ManagedPC` 里查不到他（没有 MAC 记录），所以**没法用 MAC 反查 IP**；本机在 192.168.1.6（跨网段访问
  192.168.0.x），本机 ARP 表里没有 192.168.0.x 的条目，也就拿不到 `.127` 的 MAC。
- `.127` 的端口特征（135 / 139 / 445 / 5040 开，3389 / 5985 关）和陈佳祺那台 `.179` **完全一致**，
  和机队里其它陪玩机也一样；「nbtstat 取不到 NetBIOS 名字」是全机队的普遍现象（`.179`/`.178`/`.154`/`.124`
  一样取不到），不是这台机器特殊。SMB 三种已知口令 + Administrator + 空口令全部 `STATUS_LOGON_FAILURE`
  → 这台机器上**确实没有 `chunlvops`**。
- **已确认 `.127` 就是秦伟杰那台**：修复脚本回传 `hostname=USER-20240831VS`、`ip=192.168.0.127`、
  `mac=A0-AD-9F-14-7B-F8`，同一时刻他的客户端心跳恢复、版本升到 `1.0.20260930`，
  我们也能用新账号 `chunlvops / Chunlv!xhqmvt2` 登录（看到 ADMIN$/C$/D$/IPC$）。

**改了什么（`scripts/repair-companion.ps1` 新增 `[1/7]` 步，已上传云端）：** 修复脚本现在会顺手把远程管理配好 ——
建 / 补齐 `chunlvops`（管理员组、14 位随机密码、密码永不过期）→ 打开远程管理通道
（`LocalAccountTokenFilterPolicy` / `LimitBlankPasswordUse` / `LanmanServer` / 文件共享防火墙）→
把「主机名 + IP + MAC + 账号 + 密码 + 修复前版本」回传 `/api/agent/onboard-report`，
落在云服务器 `/home/ubuntu/chunlv/onboard-reports/machines.jsonl`。
**已经配好的机器不动密码**（`accountEvent=kept`），免得把本来能用的那把换掉、回传又失败就彻底进不去。
所以：只要秦伟杰双击一次修复链接，这台机器就同时「修好客户端」+「回到标准状态」，以后我能直接连进去。

其他两处同类隐患一并修掉（装机入口）：装机 bat 用 `net session` 判管理员在「Server 服务停着」的新机器上会
无限弹授权窗口 → 换成 `fltmc`；`net.exe` 不再靠 PATH 找；回传 IP 改用 `Find-NetRoute` 按「到云服务器的
实际出口网卡」取（避免 VMware / VirtualBox 虚拟网卡抢答，把错地址回传上来）。

## 2026-09-24 修复结果（秦伟杰 `192.168.0.127`，已修好并在线）

**这台机器到底坏在哪（现场记录里的直接证据，不是猜的）：**

- `C:\Program Files\陪玩管理\陪玩管理.exe` 只有 **97,294,117 字节、版本号是空的** -> Windows 直接报
  「不是有效的 Win32 应用程序」，双击必然没反应。正常那份是 177,101,312 字节、版本 `1.0.20260930`。
  这就是「部分下载 / 部分写入」留下的半截文件，跟 09-23 修掉的「下发更新包塞烂字节」对得上。
- 同一台机上 `C:\Program Files\蠢驴电竞\陪玩管理.exe` 是 **177,101,312 字节、版本 1.0.20260865** --
  老目录里那份是好的，说明是**后来更新到「陪玩管理」目录那次写坏的**。
- 看门狗一直在空转：`service.log` 里每 4~5 秒一条 `Client PID gone - relaunching` +
  `%1 is not a valid Win32 application`，从 09-23 03:22 起没停过。老看门狗只负责「拉起来」，
  拉不起来也不会自愈重装，所以机器就一直停在「点不开」。
- `client-healthy.json` 不存在 -- 客户端从来没成功启动过。

**老板双击了两次，第一次没跑完（重要教训）：** 02:41 那次建好账号（`accountEvent=created`）之后就没了下文，
第二次现场回传里 `SystemHelper status=Stopped` -- 也就是说**窗口被中途关掉时，看门狗被留在「已停止」**，
客户端再也没人拉，机器看起来就是「又坏了」，一直持续到 02:47 第二次跑完才恢复。
已修：`repair-companion.ps1` 停看门狗之后立刻装一层保险（标记文件 + 一个盯着它的小进程），
标记没了 / 最多 45 分钟就把看门狗拉回来；正常跑完会主动撤掉保险（本机已验证：会等、会自己退干净、绝不误动正在跑的看门狗）。

**修完之后的状态（远程实测）：** 客户端 exe 177,101,312 字节 / `1.0.20260930`、看门狗 `2026-09-23.3` Running、
桌面图标指向正确、客户端 4 个进程在跑、`client-healthy.json` 已写入、
看门狗日志出现 `client health marker seen`（自愈已就绪）。
旧目录留在 `C:\Program Files\陪玩管理.broken-20260924-024716`（证据/回滚用，确认没问题后可删）。
