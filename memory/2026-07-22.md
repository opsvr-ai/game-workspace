# 2026-07-22

## 实时同步 & 权限边界 — 剩余缺口修复 (feature/chat-3.0)

**背景:** 基于 `lively-leaping-blanket.md` 计划，补充之前 `63d794bd` 会话未完成的 7 个缺口。

**修复:**

1. **republish** — 添加 `this.wsGateway.broadcastToStudio(order.studioId, 'order:pool_updated', newOrder)`
2. **renew** — 添加 `broadcastToStudio` pool 广播
3. **updateAmount** — 添加状态机守卫（仅允许 GRABBED/CONFIRMED 修改金额）+ broadcastToStudio
4. **updateContact** — 添加 `broadcastToStudio(updated.studioId, 'order:pool_updated', updated)`
5. **cancel (CS2)** — 三层传递 studioId：controller `@Req() req` → service → workflow `cancel(orderId, userStudioId?)`，添加 `if (userStudioId && order.studioId !== userStudioId)` 校验
6. **assign (CS3)** — 同上三层传递 studioId，dispatch `assign()` 添加跨工作室校验
7. **complete (CS1)** — controller 补充 `@Req()` 使已有 workflow studioId 校验生效
8. **callPartner** — 添加所有权检查 `if (order.companionId !== callerId)`
9. **acceptPartner** — 防止自协作 `if (order.companionId === partnerId)`

**涉及文件:** `orders.controller.ts`, `orders.service.ts`, `order-workflow.service.ts`, `order-dispatch.service.ts`

**验证:** 服务端 TypeScript 编译零错误，`/api/health` 正常返回。
